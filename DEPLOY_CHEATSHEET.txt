═══════════════════════════════════════════════════════════════
  MILLENNIUM SMARTBOARD - DEPLOYMENT CHEAT SHEET
  Copy these EXACT values when deploying
═══════════════════════════════════════════════════════════════

📋 BASIC SETTINGS:
──────────────────────────────────────────────────────────────
  Service Name:     millennium-smartboard
  Runtime:          Node
  Branch:           main
  Root Directory:   ./   (or leave blank)
  Region:           Oregon (US West) or closest to you


🔨 BUILD SETTINGS:
──────────────────────────────────────────────────────────────
  Build Command (copy exactly):
  
  npm install && npm run build && npm run db:seed


▶️ START SETTINGS:
──────────────────────────────────────────────────────────────
  Start Command (copy exactly):
  
  npm start


🔄 AUTO-DEPLOY:
──────────────────────────────────────────────────────────────
  Auto-Deploy:      ✅ YES (check the box)


🔧 ENVIRONMENT VARIABLES:
──────────────────────────────────────────────────────────────
  Key               Value
  ────────────────────────────────────────
  NODE_ENV          production


🏥 HEALTH CHECK (Optional but recommended):
──────────────────────────────────────────────────────────────
  Health Check Path:  /health


═══════════════════════════════════════════════════════════════
  THAT'S IT! Deploy and your app will be live! 🚀
═══════════════════════════════════════════════════════════════

📝 WHAT EACH COMMAND DOES:
  
  npm install           → Downloads dependencies
  npm run build         → Compiles TypeScript to JavaScript
  npm run db:seed       → Creates database + adds 10 customers
  npm start             → Runs your server


✅ AFTER DEPLOYMENT:
  
  Your app will be at: https://millennium-smartboard.onrender.com
  Login with:          admin / Admin@2026!
  Test:                Register device → See 10 customers in dropdown!


⚠️ REMEMBER:
  
  • NO quotes in commands
  • Copy commands EXACTLY as shown
  • Enable Auto-Deploy checkbox
  • Wait 3-5 minutes for first deployment


🆘 IF IT FAILS:
  
  Check these common mistakes:
  ✗ Quotes in build command       → Remove them
  ✗ "npm run start" instead       → Change to "npm start"
  ✗ Missing db:seed in build      → Add it back
  ✗ Wrong branch selected         → Use "main" branch
